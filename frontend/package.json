{
  "name": "engine-console-frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Zero-dependency, zero-build SPA. This file exists only so Node treats js/ as ES modules and `npm test` works; there are no dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "mock": "node dev/mock-server.mjs"
  }
}
